# Domain this service is reachable at — used to build proxy URLs returned by /register
# In ECS, set this to your internal ALB DNS name or private domain
BASE_URL=http://localhost:8000

# Redis connection URL — if unset, falls back to in-memory storage (single instance only)
# REDIS_URL=redis://localhost:6379
